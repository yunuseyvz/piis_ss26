# FlowQuest runtime requirements.
# Pinned with lower bounds only; Coolify rebuilds will pick up patch updates.

# JupyterLab itself (matches the version the extension was built against).
jupyterlab>=4.5.0,<5
jupyter_server>=2.14

# Assistant backend dependencies (LLM + env loading).
openai>=1.78.1
python-dotenv>=1.1.0

# Notebook data-science stack — matches the project's pyproject.toml so the
# seeded notebooks run without further installs.
ipykernel>=6.29.5
matplotlib>=3.10.3
numpy>=2.2.5
pandas>=2.2.3
plotly>=6.0.1
scikit-learn>=1.6.1
seaborn>=0.13.2
